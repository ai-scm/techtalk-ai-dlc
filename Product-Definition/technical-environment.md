# Technical Environment Document — App de Adopción de Mascotas

Generated by aidlc-discovery on 2026-06-21T01:30:00Z.
Project type: Greenfield

---

## Project Technical Summary

- Project Name: App de Adopción de Mascotas
- Project Type: Greenfield
- Primary Runtime Environment: Solo nube (T1)
- Cloud Provider: AWS (T2)
- Target Deployment Model: Serverless — AWS Lambda + API Gateway + DynamoDB (T3)
- Team Size and Experience: No capturado en el Quick pass (T4 corresponde a profundidad Full).

---

## Programming Languages

### Required Languages

| Lenguaje | Versión | Propósito | Justificación |
| --- | --- | --- | --- |
| Python | 3.12.x | Backend con FastAPI en AWS Lambda | APIs rápidas y simples para la POC, buena legibilidad, validación de datos y lógica de negocio sencilla |
| TypeScript | 5.x | Frontend con React | Tipado estático, menos errores, mantenimiento de componentes/formularios/llamadas al backend |
| TypeScript | 5.x | IaC con AWS CDK | Infra versionada, reutilizable y consistente con el lenguaje del frontend |
| HTML | HTML5 | Estructura de vistas del frontend | Pantallas semánticas y accesibles (catálogo, detalle, formularios, gestión de solicitudes) |
| CSS | CSS3 / Tailwind CSS | Estilos, diseño responsive y UX | Interfaz moderna y adaptable a móviles |
| JSON | Estándar | Intercambio de datos frontend↔backend | Comunicación estructurada entre React y FastAPI |

### Permitted Languages

None — solo los lenguajes requeridos arriba se consideran para la POC.

### Prohibited Languages

None explicitly prohibited (T7 = Ninguno).

---

## Frameworks and Libraries

### Required Frameworks

| Framework | Dominio | Justificación |
| --- | --- | --- |
| FastAPI | Backend / API | APIs REST en Python; validación de datos, OpenAPI automático, despliegue serverless |
| React | Frontend | Interfaz modular y dinámica para catálogo, formularios y gestión de solicitudes |
| Tailwind CSS | Frontend / Estilos | Interfaz moderna, responsive y consistente con poco esfuerzo de diseño |
| AWS CDK | IaC | Infraestructura versionada en TypeScript, despliegues repetibles |
| Pydantic | Backend / Validación | Validación de modelos de entrada/salida en FastAPI |

### Preferred Frameworks

Not applicable — no se capturaron frameworks "preferidos" adicionales en el Quick pass (T9 corresponde a profundidad Full).

### Prohibited Libraries

None — para la POC no hay librerías prohibidas (T10). Se priorizarán dependencias mantenidas, livianas y compatibles con FastAPI/React/AWS, evaluando cada una por mantenimiento, seguridad y compatibilidad.

---

## Cloud Services

### Allow List

| Service | Constraints / Notes |
| --- | --- |
| AWS Lambda | Cómputo del backend (funciones FastAPI) |
| Amazon API Gateway | Exposición de la API REST |
| Amazon DynamoDB | Almacenamiento principal (NoSQL) |
| Amazon Cognito | Autenticación y gestión de roles de usuario |
| AWS Secrets Manager / Parameter Store | Gestión de secretos |
| Amazon S3 | Almacenamiento de imágenes de mascotas (derivado de la Visión: fotos) |

Nota: lista inferida del modelo serverless elegido; a confirmar/ampliar durante AI-DLC Infrastructure Design. Las preguntas explícitas de allow/disallow (T11/T12) corresponden a profundidad Full y no se capturaron en el Quick pass.

### Disallow List

Not applicable — no se capturó lista de servicios prohibidos en el Quick pass (T12).

---

## Architecture and Patterns

### API Style

REST, descrita con OpenAPI (T13). FastAPI genera la especificación OpenAPI automáticamente, expuesta vía API Gateway.

### Data Patterns

Documento / NoSQL con Amazon DynamoDB (T14), como almacén principal de mascotas, usuarios y solicitudes de adopción.

Estrategia de consulta/filtrado del catálogo (decisión OQ-T-1, resuelta): diseño orientado a **access patterns** con **GSIs simples**, sin OpenSearch ni Aurora Serverless en el MVP.

- **Atributos normalizados**: `species`, `size`, `ageGroup`, `location`, `status`, `createdAt`.
- **Estados de publicación**: `AVAILABLE`, `IN_PROCESS`, `ADOPTED`.
- **GSIs previstos**: `status + species`, `status + location`, y un compuesto para `especie + ubicación`.
- **Filtros secundarios** (tamaño, edad): aplicados en backend (FastAPI) sobre conjuntos ya acotados por la consulta principal. No se permiten scans completos de tabla como estrategia principal.

Patrones de acceso del MVP:

| Patrón de acceso | Estrategia |
| --- | --- |
| Ver mascotas disponibles | Consulta por `status = AVAILABLE` |
| Filtrar por especie | GSI `status + species` |
| Filtrar por ubicación | GSI `status + location` |
| Filtrar por especie y ubicación | GSI compuesto o sort key compuesta |
| Filtrar por tamaño o edad | Filtro secundario sobre resultados acotados |
| Ver detalle de mascota | Consulta directa por `petId` |
| Ver mascotas de un usuario/fundación | Consulta por `publisherId` |

Evolución futura (fuera del MVP): OpenSearch para búsqueda por texto libre, filtros combinados dinámicos o geobúsqueda avanzada; Aurora Serverless / RDS para reportes relacionales complejos o transaccionalidad fuerte.

### Messaging / Integration

Not applicable en el MVP — no se capturó (T15 es profundidad Full). Comunicación principal síncrona request/response (REST) entre frontend y backend.

### Project Structure

No capturado en el Quick pass (T16 es profundidad Full). Sugerencia a confirmar en AI-DLC: separación frontend (React) / backend (FastAPI por capas: routers / services / repositories) / infra (CDK).

---

## Security

### Authentication

OAuth2 / OIDC mediante Amazon Cognito (T17). Cognito gestiona registro, inicio de sesión y los roles de usuario (adoptante, persona que publica, fundación) sin construir un servicio de autenticación propio.

### Encryption

No capturado explícitamente en el Quick pass (T18 es profundidad Full). Recomendación por defecto a confirmar en AI-DLC: cifrado en reposo y en tránsito (TLS en API Gateway; cifrado en DynamoDB y S3).

### Input Validation

No capturado explícitamente (T19 es profundidad Full). Implícito por el stack: validación de esquema en el borde de la API con Pydantic (FastAPI).

### Secrets Management

AWS Secrets Manager / Parameter Store (T20). No se versionan secretos en el repositorio.

### Compliance

No capturado en el Quick pass (T21 es profundidad Full). Al ser una POC sin datos regulados explícitos, se asume "ninguno" por ahora; a confirmar en AI-DLC.

---

## Testing

### Test Types

Unitarias, Integración y End-to-end (T22: A, B, D).

### Coverage Targets

No capturado en el Quick pass (T23 es profundidad Full). A definir en AI-DLC.

### Tooling

No capturado explícitamente (T24 es profundidad Full). Sugerencia a confirmar: pytest (unitarias/integración backend), Vitest/Jest + React Testing Library (frontend), Playwright o Cypress (e2e).

### CI/CD Gates

No capturado en el Quick pass (T25 es profundidad Full). A definir en AI-DLC.

---

## Example Code

No se capturaron patrones de código de ejemplo en el Quick pass (T26–T29 corresponden a profundidad Full).

Nota de impacto: sin al menos un patrón de endpoint/función/test de referencia, la calidad de la generación de código en AI-DLC puede reducirse. Recomendación: aportar un ejemplo canónico de endpoint FastAPI y de test pytest antes o durante la fase de Code Generation.

### Endpoint

Not provided — usar convenciones de FastAPI por defecto (a refinar en AI-DLC).

### Function / Module

Not provided — usar convenciones por defecto.

### Test

Not provided — usar convenciones por defecto (pytest).

### Infrastructure

Not provided — IaC con AWS CDK en TypeScript (a definir patrón en AI-DLC).

---

## Open Questions (Technical)

| ID | Título | Estado |
| --- | --- | --- |
| OQ-T-1 | Estrategia de consulta/filtrado del catálogo sobre DynamoDB (GSIs / índice de búsqueda / relacional) | ✅ Resuelta — DynamoDB + GSIs simples + filtrado en backend; OpenSearch/Aurora como evolución futura |

Detalle completo en `Product-Definition/open-questions.md`.
